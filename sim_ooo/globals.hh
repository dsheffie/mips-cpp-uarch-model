#ifndef __GLOBALSH__
#define __GLOBALSH__

namespace global {
  extern bool enClockFuncts;
  extern int sysArgc;
  extern char **sysArgv;
  extern std::ostream *sim_log;
  extern bool use_interp_check;
};

#endif
